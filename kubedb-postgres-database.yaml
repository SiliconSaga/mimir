apiVersion: kubedb.com/v1alpha2
kind: Postgres
metadata:
  name: my_database
spec:
  version: "15.3"
  storage:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
  databaseSecret:
    secretName: my-db-auth 