# Apply a MongoDB instance claim
apiVersion: database.example.org/v1alpha1
kind: MongoDBInstance
metadata:
  name: mongo-e2e-test
  labels:
    mimir.siliconsaga.org/test: "true"
spec:
  parameters:
    storageSize: 1Gi
    version: "6.0.24-19"
    replicas: 1

